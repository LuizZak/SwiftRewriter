import Intentions

extension CallGraph {
    static func _fromIntentions(_ collection: IntentionCollection) -> CallGraph {
        let graph = CallGraph(nodes: [], edges: [])

        return graph
    }
}
